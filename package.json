{
  "name": "industrial-orchestrator-root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm install --prefix industrial-orchestrator/server && npm install --prefix industrial-orchestrator/client && npm run build --prefix industrial-orchestrator/client && node industrial-orchestrator/server/index.js",
    "build": "echo 'Build handled in start script'"
  },
  "engines": {
    "node": "18.x"
  }
}
